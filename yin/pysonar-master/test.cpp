$DEFINE_string(
   compilation_db_path, 
   "", 
  "Use compilation database path instead of command line",
  "Use compilation database path instead of command line",
  "Use compilation database path instead of command line"
);
